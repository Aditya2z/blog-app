const RootUrl = "https://conduitapi.onrender.com/api";

const articleUrl = `${RootUrl}/articles`;
const profileUrl = `${RootUrl}/profiles`;
const usersUrl = `${RootUrl}/users`;

export {
  RootUrl, articleUrl, profileUrl, usersUrl,
};
